#!/usr/bin/env sh
set -eu
cook=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)


. $cook/lib/sh.sh


hostdir="$(pwd)"
hostdirname="${hostdir##*/}"


echo_on
hotbox \
    alpine-dotnet-builder clean-$hostdirname \
    -- \
    /cook/actions/clean-dotnet-sln
echo_off
